# 今日你的牛 — 项目概览

## 完成内容

根据《今日你的牛_PRD.md》完成了 MVP 版本的完整前端网站，包含所有第一版必做功能和推荐功能。

### 已实现功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 首页（极简召唤入口） | 已完成 | 牛视觉 + 产品名 + 情绪文案 + 召唤按钮 + 社会证明 |
| 抽取动画 | 已完成 | 1.8 秒滚动动画，快速滚动 → 减速 → 停在最终牛 |
| 54 种牛人格 | 已完成 | 49 普通 + 5 稀有 = 54 种，每类 9 种，覆盖 6 大类 |
| 九宫格视觉素材 | 已完成 | 6 张 3×3 透明原图按 row / col 精确切片，覆盖全部 54 种牛 |
| 每日固定结果 | 已完成 | 基于 `hash(deviceId + yyyyMMdd)` 生成当日固定结果 |
| 牛气值 | 已完成 | 0-100% 动画进度条 |
| 标签 + 文案 | 已完成 | 每种牛 3 个标签 + 2-3 条文案变体 |
| 结果页 | 已完成 | 牛名 + 稀有度 + 牛气值 + 标签 + 文案 + 操作按钮 |
| 今日牛卡 | 已完成 | 3:4 比例，含编号、日期、扫码提示 |
| 保存图片 | 已完成 | Canvas API 生成 1080x1440 PNG 可下载 |
| 分享链接 | 已完成 | URL 参数传递好友牛信息 + 分享文案自动生成 |
| 偷偷换牛卡 | 已完成 | 每天 5 次，确认弹窗 |
| 好友入口页 | 已完成 | 展示朋友的牛 + "现在轮到你了" |
| 再次访问读取 | 已完成 | 当天再次进入直接展示结果 |
| 稀有牛体系 | 已完成 | N/R/SR/SSR/UR 五级 + 稀有牛横幅提示 |
| 情境加权 | 已完成 | 工作日上午/下午/深夜/周末/周五等时段动态调权 |

### 技术栈

- 纯 HTML + CSS + JavaScript（无框架依赖）
- LocalStorage 存储设备 ID 和每日结果
- Canvas API 生成可下载牛卡图片
- CSS 切片视窗 + Canvas 源区域裁切，共用 6 张九宫格透明素材

### 设计系统

- **主色**：牛气红橙 `#D9482E`
- **背景**：温暖奶油色 `#FBF7F0`
- **字体**：Noto Serif SC（标题，东方韵味）+ 系统无衬线（正文）
- **稀有度色阶**：N灰 / R蓝 / SR紫 / SSR金 / UR彩虹渐变
- **动画**：入场逐项淡入、召唤按钮脉冲、牛图标浮动、抽取滚动减速、结果弹出弹性

### 文件结构

```
今日你的牛/
├── index.html          # 主页面（4 个屏幕 + 2 个模态框）
├── css/style.css       # 完整设计系统
├── images/             # 6 类 3×3 透明九宫格牛图素材
├── js/
│   ├── bulls-data.js   # 54 种牛数据 + 九宫格行列映射
│   └── app.js          # 核心逻辑（抽取算法/动画/牛卡/分享）
└── scripts/serve.py    # 守护进程静态服务器
```

### 本地预览

服务器已启动：http://127.0.0.1:8088

### PRD 对照

- 第一版必做 11 项：全部完成
- 推荐做 5 项：全部完成
- 第一版暂不做：均未实现（符合 MVP 范围）

### 后续可扩展

- 牛图鉴（V2）
- 连续召牛记录 / 牛历（V2）
- 今日牛搭子组合（V2）
- 节日运营换皮（只需更新数据文件）
